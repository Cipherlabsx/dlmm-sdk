/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const RECONCILE_POOL_DRIFT_DISCRIMINATOR = new Uint8Array([
  61, 105, 25, 71, 110, 94, 194, 66,
]);

export function getReconcilePoolDriftDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECONCILE_POOL_DRIFT_DISCRIMINATOR,
  );
}

export type ReconcilePoolDriftInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountBaseVault extends string | AccountMeta<string> = string,
  TAccountQuoteVault extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountBaseVault extends string
        ? ReadonlyAccount<TAccountBaseVault>
        : TAccountBaseVault,
      TAccountQuoteVault extends string
        ? ReadonlyAccount<TAccountQuoteVault>
        : TAccountQuoteVault,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type ReconcilePoolDriftInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReconcilePoolDriftInstructionDataArgs = {};

export function getReconcilePoolDriftInstructionDataEncoder(): FixedSizeEncoder<ReconcilePoolDriftInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: RECONCILE_POOL_DRIFT_DISCRIMINATOR,
    }),
  );
}

export function getReconcilePoolDriftInstructionDataDecoder(): FixedSizeDecoder<ReconcilePoolDriftInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReconcilePoolDriftInstructionDataCodec(): FixedSizeCodec<
  ReconcilePoolDriftInstructionDataArgs,
  ReconcilePoolDriftInstructionData
> {
  return combineCodec(
    getReconcilePoolDriftInstructionDataEncoder(),
    getReconcilePoolDriftInstructionDataDecoder(),
  );
}

export type ReconcilePoolDriftInput<
  TAccountPool extends string = string,
  TAccountBaseVault extends string = string,
  TAccountQuoteVault extends string = string,
  TAccountAdmin extends string = string,
> = {
  /** Pool to reconcile (mutable - we read admin/vaults). */
  pool: Address<TAccountPool>;
  baseVault: Address<TAccountBaseVault>;
  quoteVault: Address<TAccountQuoteVault>;
  /** Admin signer - only admin can reconcile. */
  admin: TransactionSigner<TAccountAdmin>;
};

export function getReconcilePoolDriftInstruction<
  TAccountPool extends string,
  TAccountBaseVault extends string,
  TAccountQuoteVault extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: ReconcilePoolDriftInput<
    TAccountPool,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountAdmin
  >,
  config?: { programAddress?: TProgramAddress },
): ReconcilePoolDriftInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountBaseVault,
  TAccountQuoteVault,
  TAccountAdmin
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    baseVault: { value: input.baseVault ?? null, isWritable: false },
    quoteVault: { value: input.quoteVault ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("baseVault", accounts.baseVault),
      getAccountMeta("quoteVault", accounts.quoteVault),
      getAccountMeta("admin", accounts.admin),
    ],
    data: getReconcilePoolDriftInstructionDataEncoder().encode({}),
    programAddress,
  } as ReconcilePoolDriftInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountAdmin
  >);
}

export type ParsedReconcilePoolDriftInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool to reconcile (mutable - we read admin/vaults). */
    pool: TAccountMetas[0];
    baseVault: TAccountMetas[1];
    quoteVault: TAccountMetas[2];
    /** Admin signer - only admin can reconcile. */
    admin: TAccountMetas[3];
  };
  data: ReconcilePoolDriftInstructionData;
};

export function parseReconcilePoolDriftInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReconcilePoolDriftInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      baseVault: getNextAccount(),
      quoteVault: getNextAccount(),
      admin: getNextAccount(),
    },
    data: getReconcilePoolDriftInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
