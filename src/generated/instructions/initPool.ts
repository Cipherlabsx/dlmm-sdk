/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";
import {
  getFeeConfigDecoder,
  getFeeConfigEncoder,
  type FeeConfig,
  type FeeConfigArgs,
} from "../types";

export const INIT_POOL_DISCRIMINATOR = new Uint8Array([
  116, 233, 199, 204, 115, 159, 171, 36,
]);

export function getInitPoolDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_POOL_DISCRIMINATOR);
}

export type InitPoolInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountBaseMintAccount extends string | AccountMeta<string> = string,
  TAccountQuoteMintAccount extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountLpMint extends string | AccountMeta<string> = string,
  TAccountBaseVault extends string | AccountMeta<string> = string,
  TAccountQuoteVault extends string | AccountMeta<string> = string,
  TAccountCreatorFeeVault extends string | AccountMeta<string> = string,
  TAccountHoldersFeeVault extends string | AccountMeta<string> = string,
  TAccountNftFeeVault extends string | AccountMeta<string> = string,
  TAccountProtocolFeeVault extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountCreator extends string
        ? ReadonlyAccount<TAccountCreator>
        : TAccountCreator,
      TAccountBaseMintAccount extends string
        ? ReadonlyAccount<TAccountBaseMintAccount>
        : TAccountBaseMintAccount,
      TAccountQuoteMintAccount extends string
        ? ReadonlyAccount<TAccountQuoteMintAccount>
        : TAccountQuoteMintAccount,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountLpMint extends string
        ? WritableSignerAccount<TAccountLpMint> &
            AccountSignerMeta<TAccountLpMint>
        : TAccountLpMint,
      TAccountBaseVault extends string
        ? WritableAccount<TAccountBaseVault>
        : TAccountBaseVault,
      TAccountQuoteVault extends string
        ? WritableAccount<TAccountQuoteVault>
        : TAccountQuoteVault,
      TAccountCreatorFeeVault extends string
        ? WritableAccount<TAccountCreatorFeeVault>
        : TAccountCreatorFeeVault,
      TAccountHoldersFeeVault extends string
        ? WritableAccount<TAccountHoldersFeeVault>
        : TAccountHoldersFeeVault,
      TAccountNftFeeVault extends string
        ? WritableAccount<TAccountNftFeeVault>
        : TAccountNftFeeVault,
      TAccountProtocolFeeVault extends string
        ? WritableAccount<TAccountProtocolFeeVault>
        : TAccountProtocolFeeVault,
      TAccountRegistry extends string
        ? WritableAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  baseMint: Address;
  quoteMint: Address;
  binStepBps: number;
  initialPriceQ6464: bigint;
  feeConfig: FeeConfig;
  accountingMode: number;
};

export type InitPoolInstructionDataArgs = {
  baseMint: Address;
  quoteMint: Address;
  binStepBps: number;
  initialPriceQ6464: number | bigint;
  feeConfig: FeeConfigArgs;
  accountingMode: number;
};

export function getInitPoolInstructionDataEncoder(): FixedSizeEncoder<InitPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["baseMint", getAddressEncoder()],
      ["quoteMint", getAddressEncoder()],
      ["binStepBps", getU16Encoder()],
      ["initialPriceQ6464", getU128Encoder()],
      ["feeConfig", getFeeConfigEncoder()],
      ["accountingMode", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_POOL_DISCRIMINATOR }),
  );
}

export function getInitPoolInstructionDataDecoder(): FixedSizeDecoder<InitPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["baseMint", getAddressDecoder()],
    ["quoteMint", getAddressDecoder()],
    ["binStepBps", getU16Decoder()],
    ["initialPriceQ6464", getU128Decoder()],
    ["feeConfig", getFeeConfigDecoder()],
    ["accountingMode", getU8Decoder()],
  ]);
}

export function getInitPoolInstructionDataCodec(): FixedSizeCodec<
  InitPoolInstructionDataArgs,
  InitPoolInstructionData
> {
  return combineCodec(
    getInitPoolInstructionDataEncoder(),
    getInitPoolInstructionDataDecoder(),
  );
}

export type InitPoolAsyncInput<
  TAccountAdmin extends string = string,
  TAccountCreator extends string = string,
  TAccountBaseMintAccount extends string = string,
  TAccountQuoteMintAccount extends string = string,
  TAccountPool extends string = string,
  TAccountLpMint extends string = string,
  TAccountBaseVault extends string = string,
  TAccountQuoteVault extends string = string,
  TAccountCreatorFeeVault extends string = string,
  TAccountHoldersFeeVault extends string = string,
  TAccountNftFeeVault extends string = string,
  TAccountProtocolFeeVault extends string = string,
  TAccountRegistry extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Pays for initialization, becomes pool admin (can be rotated later). */
  admin: TransactionSigner<TAccountAdmin>;
  creator: Address<TAccountCreator>;
  /** Base mint (validated by token::mint constraint on vaults) */
  baseMintAccount: Address<TAccountBaseMintAccount>;
  /** Quote mint (validated by token::mint constraint on vaults) */
  quoteMintAccount: Address<TAccountQuoteMintAccount>;
  /**
   * Pool state account (PDA), zero_copy for stack efficiency.
   * CRITICAL: Pool address includes BOTH bin_step_bps AND base_fee_bps to allow multiple pools
   * per token pair with different fee tiers and price granularities.
   * This enables traders to choose between tighter spreads (lower bin steps) or wider ranges (higher bin steps),
   * AND choose between different fee tiers (e.g., 0.3% vs 0.4% base fees).
   */
  pool: Address<TAccountPool>;
  /** LP mint (fungible shares) */
  lpMint: TransactionSigner<TAccountLpMint>;
  /** Base vault - stores base token liquidity (Box to save stack space) */
  baseVault?: Address<TAccountBaseVault>;
  /** Quote vault - stores quote token liquidity (Box to save stack space) */
  quoteVault?: Address<TAccountQuoteVault>;
  /** Creator fee vault - stores creator's share of swap fees (Box to save stack space) */
  creatorFeeVault?: Address<TAccountCreatorFeeVault>;
  /** Holders fee vault - stores LP holders' share of swap fees (Box to save stack space) */
  holdersFeeVault?: Address<TAccountHoldersFeeVault>;
  /** NFT fee vault - stores NFT holders' share of swap fees (Box to save stack space) */
  nftFeeVault?: Address<TAccountNftFeeVault>;
  /**
   * Protocol fee vault - stores protocol's share (12.5% of swap fees) (Box to save stack space)
   * Can be permissionlessly swept to Squads multisig via transfer_protocol_fees instruction
   */
  protocolFeeVault?: Address<TAccountProtocolFeeVault>;
  /**
   * Pair registry PDA to prevent duplicate pools.
   * CRITICAL: Includes BOTH bin_step_bps AND base_fee_bps to allow multiple pools per token pair
   * with different fee tiers (one registry per unique pool configuration)
   */
  registry: Address<TAccountRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  baseMint: InitPoolInstructionDataArgs["baseMint"];
  quoteMint: InitPoolInstructionDataArgs["quoteMint"];
  binStepBps: InitPoolInstructionDataArgs["binStepBps"];
  initialPriceQ6464: InitPoolInstructionDataArgs["initialPriceQ6464"];
  feeConfig: InitPoolInstructionDataArgs["feeConfig"];
  accountingMode: InitPoolInstructionDataArgs["accountingMode"];
};

export async function getInitPoolInstructionAsync<
  TAccountAdmin extends string,
  TAccountCreator extends string,
  TAccountBaseMintAccount extends string,
  TAccountQuoteMintAccount extends string,
  TAccountPool extends string,
  TAccountLpMint extends string,
  TAccountBaseVault extends string,
  TAccountQuoteVault extends string,
  TAccountCreatorFeeVault extends string,
  TAccountHoldersFeeVault extends string,
  TAccountNftFeeVault extends string,
  TAccountProtocolFeeVault extends string,
  TAccountRegistry extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitPoolAsyncInput<
    TAccountAdmin,
    TAccountCreator,
    TAccountBaseMintAccount,
    TAccountQuoteMintAccount,
    TAccountPool,
    TAccountLpMint,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountCreatorFeeVault,
    TAccountHoldersFeeVault,
    TAccountNftFeeVault,
    TAccountProtocolFeeVault,
    TAccountRegistry,
    TAccountSystemProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitPoolInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountCreator,
    TAccountBaseMintAccount,
    TAccountQuoteMintAccount,
    TAccountPool,
    TAccountLpMint,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountCreatorFeeVault,
    TAccountHoldersFeeVault,
    TAccountNftFeeVault,
    TAccountProtocolFeeVault,
    TAccountRegistry,
    TAccountSystemProgram,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
    baseMintAccount: {
      value: input.baseMintAccount ?? null,
      isWritable: false,
    },
    quoteMintAccount: {
      value: input.quoteMintAccount ?? null,
      isWritable: false,
    },
    pool: { value: input.pool ?? null, isWritable: true },
    lpMint: { value: input.lpMint ?? null, isWritable: true },
    baseVault: { value: input.baseVault ?? null, isWritable: true },
    quoteVault: { value: input.quoteVault ?? null, isWritable: true },
    creatorFeeVault: { value: input.creatorFeeVault ?? null, isWritable: true },
    holdersFeeVault: { value: input.holdersFeeVault ?? null, isWritable: true },
    nftFeeVault: { value: input.nftFeeVault ?? null, isWritable: true },
    protocolFeeVault: {
      value: input.protocolFeeVault ?? null,
      isWritable: true,
    },
    registry: { value: input.registry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.baseVault.value) {
    accounts.baseVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getBytesEncoder().encode(new Uint8Array([98, 97, 115, 101])),
      ],
    });
  }
  if (!accounts.quoteVault.value) {
    accounts.quoteVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getBytesEncoder().encode(new Uint8Array([113, 117, 111, 116, 101])),
      ],
    });
  }
  if (!accounts.creatorFeeVault.value) {
    accounts.creatorFeeVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getBytesEncoder().encode(
          new Uint8Array([99, 114, 101, 97, 116, 111, 114, 95, 102, 101, 101]),
        ),
      ],
    });
  }
  if (!accounts.holdersFeeVault.value) {
    accounts.holdersFeeVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            104, 111, 108, 100, 101, 114, 115, 95, 102, 101, 101,
          ]),
        ),
      ],
    });
  }
  if (!accounts.nftFeeVault.value) {
    accounts.nftFeeVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getBytesEncoder().encode(
          new Uint8Array([110, 102, 116, 95, 102, 101, 101]),
        ),
      ],
    });
  }
  if (!accounts.protocolFeeVault.value) {
    accounts.protocolFeeVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            112, 114, 111, 116, 111, 99, 111, 108, 95, 102, 101, 101,
          ]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("baseMintAccount", accounts.baseMintAccount),
      getAccountMeta("quoteMintAccount", accounts.quoteMintAccount),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("lpMint", accounts.lpMint),
      getAccountMeta("baseVault", accounts.baseVault),
      getAccountMeta("quoteVault", accounts.quoteVault),
      getAccountMeta("creatorFeeVault", accounts.creatorFeeVault),
      getAccountMeta("holdersFeeVault", accounts.holdersFeeVault),
      getAccountMeta("nftFeeVault", accounts.nftFeeVault),
      getAccountMeta("protocolFeeVault", accounts.protocolFeeVault),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getInitPoolInstructionDataEncoder().encode(
      args as InitPoolInstructionDataArgs,
    ),
    programAddress,
  } as InitPoolInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountCreator,
    TAccountBaseMintAccount,
    TAccountQuoteMintAccount,
    TAccountPool,
    TAccountLpMint,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountCreatorFeeVault,
    TAccountHoldersFeeVault,
    TAccountNftFeeVault,
    TAccountProtocolFeeVault,
    TAccountRegistry,
    TAccountSystemProgram,
    TAccountTokenProgram
  >);
}

export type InitPoolInput<
  TAccountAdmin extends string = string,
  TAccountCreator extends string = string,
  TAccountBaseMintAccount extends string = string,
  TAccountQuoteMintAccount extends string = string,
  TAccountPool extends string = string,
  TAccountLpMint extends string = string,
  TAccountBaseVault extends string = string,
  TAccountQuoteVault extends string = string,
  TAccountCreatorFeeVault extends string = string,
  TAccountHoldersFeeVault extends string = string,
  TAccountNftFeeVault extends string = string,
  TAccountProtocolFeeVault extends string = string,
  TAccountRegistry extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Pays for initialization, becomes pool admin (can be rotated later). */
  admin: TransactionSigner<TAccountAdmin>;
  creator: Address<TAccountCreator>;
  /** Base mint (validated by token::mint constraint on vaults) */
  baseMintAccount: Address<TAccountBaseMintAccount>;
  /** Quote mint (validated by token::mint constraint on vaults) */
  quoteMintAccount: Address<TAccountQuoteMintAccount>;
  /**
   * Pool state account (PDA), zero_copy for stack efficiency.
   * CRITICAL: Pool address includes BOTH bin_step_bps AND base_fee_bps to allow multiple pools
   * per token pair with different fee tiers and price granularities.
   * This enables traders to choose between tighter spreads (lower bin steps) or wider ranges (higher bin steps),
   * AND choose between different fee tiers (e.g., 0.3% vs 0.4% base fees).
   */
  pool: Address<TAccountPool>;
  /** LP mint (fungible shares) */
  lpMint: TransactionSigner<TAccountLpMint>;
  /** Base vault - stores base token liquidity (Box to save stack space) */
  baseVault: Address<TAccountBaseVault>;
  /** Quote vault - stores quote token liquidity (Box to save stack space) */
  quoteVault: Address<TAccountQuoteVault>;
  /** Creator fee vault - stores creator's share of swap fees (Box to save stack space) */
  creatorFeeVault: Address<TAccountCreatorFeeVault>;
  /** Holders fee vault - stores LP holders' share of swap fees (Box to save stack space) */
  holdersFeeVault: Address<TAccountHoldersFeeVault>;
  /** NFT fee vault - stores NFT holders' share of swap fees (Box to save stack space) */
  nftFeeVault: Address<TAccountNftFeeVault>;
  /**
   * Protocol fee vault - stores protocol's share (12.5% of swap fees) (Box to save stack space)
   * Can be permissionlessly swept to Squads multisig via transfer_protocol_fees instruction
   */
  protocolFeeVault: Address<TAccountProtocolFeeVault>;
  /**
   * Pair registry PDA to prevent duplicate pools.
   * CRITICAL: Includes BOTH bin_step_bps AND base_fee_bps to allow multiple pools per token pair
   * with different fee tiers (one registry per unique pool configuration)
   */
  registry: Address<TAccountRegistry>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenProgram?: Address<TAccountTokenProgram>;
  baseMint: InitPoolInstructionDataArgs["baseMint"];
  quoteMint: InitPoolInstructionDataArgs["quoteMint"];
  binStepBps: InitPoolInstructionDataArgs["binStepBps"];
  initialPriceQ6464: InitPoolInstructionDataArgs["initialPriceQ6464"];
  feeConfig: InitPoolInstructionDataArgs["feeConfig"];
  accountingMode: InitPoolInstructionDataArgs["accountingMode"];
};

export function getInitPoolInstruction<
  TAccountAdmin extends string,
  TAccountCreator extends string,
  TAccountBaseMintAccount extends string,
  TAccountQuoteMintAccount extends string,
  TAccountPool extends string,
  TAccountLpMint extends string,
  TAccountBaseVault extends string,
  TAccountQuoteVault extends string,
  TAccountCreatorFeeVault extends string,
  TAccountHoldersFeeVault extends string,
  TAccountNftFeeVault extends string,
  TAccountProtocolFeeVault extends string,
  TAccountRegistry extends string,
  TAccountSystemProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitPoolInput<
    TAccountAdmin,
    TAccountCreator,
    TAccountBaseMintAccount,
    TAccountQuoteMintAccount,
    TAccountPool,
    TAccountLpMint,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountCreatorFeeVault,
    TAccountHoldersFeeVault,
    TAccountNftFeeVault,
    TAccountProtocolFeeVault,
    TAccountRegistry,
    TAccountSystemProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitPoolInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountCreator,
  TAccountBaseMintAccount,
  TAccountQuoteMintAccount,
  TAccountPool,
  TAccountLpMint,
  TAccountBaseVault,
  TAccountQuoteVault,
  TAccountCreatorFeeVault,
  TAccountHoldersFeeVault,
  TAccountNftFeeVault,
  TAccountProtocolFeeVault,
  TAccountRegistry,
  TAccountSystemProgram,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
    baseMintAccount: {
      value: input.baseMintAccount ?? null,
      isWritable: false,
    },
    quoteMintAccount: {
      value: input.quoteMintAccount ?? null,
      isWritable: false,
    },
    pool: { value: input.pool ?? null, isWritable: true },
    lpMint: { value: input.lpMint ?? null, isWritable: true },
    baseVault: { value: input.baseVault ?? null, isWritable: true },
    quoteVault: { value: input.quoteVault ?? null, isWritable: true },
    creatorFeeVault: { value: input.creatorFeeVault ?? null, isWritable: true },
    holdersFeeVault: { value: input.holdersFeeVault ?? null, isWritable: true },
    nftFeeVault: { value: input.nftFeeVault ?? null, isWritable: true },
    protocolFeeVault: {
      value: input.protocolFeeVault ?? null,
      isWritable: true,
    },
    registry: { value: input.registry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("baseMintAccount", accounts.baseMintAccount),
      getAccountMeta("quoteMintAccount", accounts.quoteMintAccount),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("lpMint", accounts.lpMint),
      getAccountMeta("baseVault", accounts.baseVault),
      getAccountMeta("quoteVault", accounts.quoteVault),
      getAccountMeta("creatorFeeVault", accounts.creatorFeeVault),
      getAccountMeta("holdersFeeVault", accounts.holdersFeeVault),
      getAccountMeta("nftFeeVault", accounts.nftFeeVault),
      getAccountMeta("protocolFeeVault", accounts.protocolFeeVault),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getInitPoolInstructionDataEncoder().encode(
      args as InitPoolInstructionDataArgs,
    ),
    programAddress,
  } as InitPoolInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountCreator,
    TAccountBaseMintAccount,
    TAccountQuoteMintAccount,
    TAccountPool,
    TAccountLpMint,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountCreatorFeeVault,
    TAccountHoldersFeeVault,
    TAccountNftFeeVault,
    TAccountProtocolFeeVault,
    TAccountRegistry,
    TAccountSystemProgram,
    TAccountTokenProgram
  >);
}

export type ParsedInitPoolInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for initialization, becomes pool admin (can be rotated later). */
    admin: TAccountMetas[0];
    creator: TAccountMetas[1];
    /** Base mint (validated by token::mint constraint on vaults) */
    baseMintAccount: TAccountMetas[2];
    /** Quote mint (validated by token::mint constraint on vaults) */
    quoteMintAccount: TAccountMetas[3];
    /**
     * Pool state account (PDA), zero_copy for stack efficiency.
     * CRITICAL: Pool address includes BOTH bin_step_bps AND base_fee_bps to allow multiple pools
     * per token pair with different fee tiers and price granularities.
     * This enables traders to choose between tighter spreads (lower bin steps) or wider ranges (higher bin steps),
     * AND choose between different fee tiers (e.g., 0.3% vs 0.4% base fees).
     */
    pool: TAccountMetas[4];
    /** LP mint (fungible shares) */
    lpMint: TAccountMetas[5];
    /** Base vault - stores base token liquidity (Box to save stack space) */
    baseVault: TAccountMetas[6];
    /** Quote vault - stores quote token liquidity (Box to save stack space) */
    quoteVault: TAccountMetas[7];
    /** Creator fee vault - stores creator's share of swap fees (Box to save stack space) */
    creatorFeeVault: TAccountMetas[8];
    /** Holders fee vault - stores LP holders' share of swap fees (Box to save stack space) */
    holdersFeeVault: TAccountMetas[9];
    /** NFT fee vault - stores NFT holders' share of swap fees (Box to save stack space) */
    nftFeeVault: TAccountMetas[10];
    /**
     * Protocol fee vault - stores protocol's share (12.5% of swap fees) (Box to save stack space)
     * Can be permissionlessly swept to Squads multisig via transfer_protocol_fees instruction
     */
    protocolFeeVault: TAccountMetas[11];
    /**
     * Pair registry PDA to prevent duplicate pools.
     * CRITICAL: Includes BOTH bin_step_bps AND base_fee_bps to allow multiple pools per token pair
     * with different fee tiers (one registry per unique pool configuration)
     */
    registry: TAccountMetas[12];
    systemProgram: TAccountMetas[13];
    tokenProgram: TAccountMetas[14];
  };
  data: InitPoolInstructionData;
};

export function parseInitPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 15) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 15,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      creator: getNextAccount(),
      baseMintAccount: getNextAccount(),
      quoteMintAccount: getNextAccount(),
      pool: getNextAccount(),
      lpMint: getNextAccount(),
      baseVault: getNextAccount(),
      quoteVault: getNextAccount(),
      creatorFeeVault: getNextAccount(),
      holdersFeeVault: getNextAccount(),
      nftFeeVault: getNextAccount(),
      protocolFeeVault: getNextAccount(),
      registry: getNextAccount(),
      systemProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getInitPoolInstructionDataDecoder().decode(instruction.data),
  };
}
