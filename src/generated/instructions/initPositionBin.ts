/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const INIT_POSITION_BIN_DISCRIMINATOR = new Uint8Array([
  249, 110, 124, 16, 185, 55, 149, 13,
]);

export function getInitPositionBinDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_POSITION_BIN_DISCRIMINATOR,
  );
}

export type InitPositionBinInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountPositionBin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountPositionBin extends string
        ? WritableSignerAccount<TAccountPositionBin> &
            AccountSignerMeta<TAccountPositionBin>
        : TAccountPositionBin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitPositionBinInstructionData = {
  discriminator: ReadonlyUint8Array;
  binIndex: bigint;
};

export type InitPositionBinInstructionDataArgs = { binIndex: number | bigint };

export function getInitPositionBinInstructionDataEncoder(): FixedSizeEncoder<InitPositionBinInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["binIndex", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_POSITION_BIN_DISCRIMINATOR }),
  );
}

export function getInitPositionBinInstructionDataDecoder(): FixedSizeDecoder<InitPositionBinInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["binIndex", getU64Decoder()],
  ]);
}

export function getInitPositionBinInstructionDataCodec(): FixedSizeCodec<
  InitPositionBinInstructionDataArgs,
  InitPositionBinInstructionData
> {
  return combineCodec(
    getInitPositionBinInstructionDataEncoder(),
    getInitPositionBinInstructionDataDecoder(),
  );
}

export type InitPositionBinInput<
  TAccountPool extends string = string,
  TAccountOwner extends string = string,
  TAccountPosition extends string = string,
  TAccountPositionBin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  owner: TransactionSigner<TAccountOwner>;
  /** Position PDA (canonical seeds), ensures owner & pool binding */
  position: Address<TAccountPosition>;
  /** PositionBin PDA (canonical) */
  positionBin: TransactionSigner<TAccountPositionBin>;
  systemProgram?: Address<TAccountSystemProgram>;
  binIndex: InitPositionBinInstructionDataArgs["binIndex"];
};

export function getInitPositionBinInstruction<
  TAccountPool extends string,
  TAccountOwner extends string,
  TAccountPosition extends string,
  TAccountPositionBin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitPositionBinInput<
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountPositionBin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitPositionBinInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountOwner,
  TAccountPosition,
  TAccountPositionBin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    positionBin: { value: input.positionBin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("position", accounts.position),
      getAccountMeta("positionBin", accounts.positionBin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitPositionBinInstructionDataEncoder().encode(
      args as InitPositionBinInstructionDataArgs,
    ),
    programAddress,
  } as InitPositionBinInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountPositionBin,
    TAccountSystemProgram
  >);
}

export type ParsedInitPositionBinInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    owner: TAccountMetas[1];
    /** Position PDA (canonical seeds), ensures owner & pool binding */
    position: TAccountMetas[2];
    /** PositionBin PDA (canonical) */
    positionBin: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitPositionBinInstructionData;
};

export function parseInitPositionBinInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitPositionBinInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      owner: getNextAccount(),
      position: getNextAccount(),
      positionBin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitPositionBinInstructionDataDecoder().decode(instruction.data),
  };
}
