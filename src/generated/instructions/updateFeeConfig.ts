/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";
import {
  getFeeConfigDecoder,
  getFeeConfigEncoder,
  type FeeConfig,
  type FeeConfigArgs,
} from "../types";

export const UPDATE_FEE_CONFIG_DISCRIMINATOR = new Uint8Array([
  104, 184, 103, 242, 88, 151, 107, 20,
]);

export function getUpdateFeeConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_FEE_CONFIG_DISCRIMINATOR,
  );
}

export type UpdateFeeConfigInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateFeeConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  newFeeConfig: FeeConfig;
};

export type UpdateFeeConfigInstructionDataArgs = {
  newFeeConfig: FeeConfigArgs;
};

export function getUpdateFeeConfigInstructionDataEncoder(): FixedSizeEncoder<UpdateFeeConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newFeeConfig", getFeeConfigEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_FEE_CONFIG_DISCRIMINATOR }),
  );
}

export function getUpdateFeeConfigInstructionDataDecoder(): FixedSizeDecoder<UpdateFeeConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newFeeConfig", getFeeConfigDecoder()],
  ]);
}

export function getUpdateFeeConfigInstructionDataCodec(): FixedSizeCodec<
  UpdateFeeConfigInstructionDataArgs,
  UpdateFeeConfigInstructionData
> {
  return combineCodec(
    getUpdateFeeConfigInstructionDataEncoder(),
    getUpdateFeeConfigInstructionDataDecoder(),
  );
}

export type UpdateFeeConfigInput<
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * Signer account used for config updates.
   * Kept as `admin` for client compatibility; validated against `pool.config_authority`.
   */
  admin: TransactionSigner<TAccountAdmin>;
  newFeeConfig: UpdateFeeConfigInstructionDataArgs["newFeeConfig"];
};

export function getUpdateFeeConfigInstruction<
  TAccountPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: UpdateFeeConfigInput<TAccountPool, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdateFeeConfigInstruction<TProgramAddress, TAccountPool, TAccountAdmin> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("admin", accounts.admin),
    ],
    data: getUpdateFeeConfigInstructionDataEncoder().encode(
      args as UpdateFeeConfigInstructionDataArgs,
    ),
    programAddress,
  } as UpdateFeeConfigInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin
  >);
}

export type ParsedUpdateFeeConfigInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * Signer account used for config updates.
     * Kept as `admin` for client compatibility; validated against `pool.config_authority`.
     */
    admin: TAccountMetas[1];
  };
  data: UpdateFeeConfigInstructionData;
};

export function parseUpdateFeeConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateFeeConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pool: getNextAccount(), admin: getNextAccount() },
    data: getUpdateFeeConfigInstructionDataDecoder().decode(instruction.data),
  };
}
