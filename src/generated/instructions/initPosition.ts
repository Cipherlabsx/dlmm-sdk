/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const INIT_POSITION_DISCRIMINATOR = new Uint8Array([
  197, 20, 10, 1, 97, 160, 177, 91,
]);

export function getInitPositionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_POSITION_DISCRIMINATOR,
  );
}

export type InitPositionInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitPositionInstructionData = {
  discriminator: ReadonlyUint8Array;
  nonce: bigint;
};

export type InitPositionInstructionDataArgs = { nonce: number | bigint };

export function getInitPositionInstructionDataEncoder(): FixedSizeEncoder<InitPositionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_POSITION_DISCRIMINATOR }),
  );
}

export function getInitPositionInstructionDataDecoder(): FixedSizeDecoder<InitPositionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU64Decoder()],
  ]);
}

export function getInitPositionInstructionDataCodec(): FixedSizeCodec<
  InitPositionInstructionDataArgs,
  InitPositionInstructionData
> {
  return combineCodec(
    getInitPositionInstructionDataEncoder(),
    getInitPositionInstructionDataDecoder(),
  );
}

export type InitPositionAsyncInput<
  TAccountPool extends string = string,
  TAccountOwner extends string = string,
  TAccountPosition extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  owner: TransactionSigner<TAccountOwner>;
  position?: Address<TAccountPosition>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: InitPositionInstructionDataArgs["nonce"];
};

export async function getInitPositionInstructionAsync<
  TAccountPool extends string,
  TAccountOwner extends string,
  TAccountPosition extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitPositionAsyncInput<
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitPositionInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.position.value) {
    accounts.position.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([112, 111, 115, 105, 116, 105, 111, 110]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "owner",
            accounts.owner.value,
          ),
        ),
        getU64Encoder().encode(
          getNonNullResolvedInstructionInput("nonce", args.nonce),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("position", accounts.position),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitPositionInstructionDataEncoder().encode(
      args as InitPositionInstructionDataArgs,
    ),
    programAddress,
  } as InitPositionInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountSystemProgram
  >);
}

export type InitPositionInput<
  TAccountPool extends string = string,
  TAccountOwner extends string = string,
  TAccountPosition extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  owner: TransactionSigner<TAccountOwner>;
  position: Address<TAccountPosition>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: InitPositionInstructionDataArgs["nonce"];
};

export function getInitPositionInstruction<
  TAccountPool extends string,
  TAccountOwner extends string,
  TAccountPosition extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitPositionInput<
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitPositionInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountOwner,
  TAccountPosition,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("position", accounts.position),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitPositionInstructionDataEncoder().encode(
      args as InitPositionInstructionDataArgs,
    ),
    programAddress,
  } as InitPositionInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountOwner,
    TAccountPosition,
    TAccountSystemProgram
  >);
}

export type ParsedInitPositionInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    owner: TAccountMetas[1];
    position: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitPositionInstructionData;
};

export function parseInitPositionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitPositionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      owner: getNextAccount(),
      position: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitPositionInstructionDataDecoder().decode(instruction.data),
  };
}
