/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const INIT_USER_HOLDER_STATE_DISCRIMINATOR = new Uint8Array([
  49, 178, 188, 199, 246, 133, 51, 222,
]);

export function getInitUserHolderStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_USER_HOLDER_STATE_DISCRIMINATOR,
  );
}

export type InitUserHolderStateInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountHolderGlobalState extends string | AccountMeta<string> = string,
  TAccountUserHolderState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountUser extends string
        ? ReadonlyAccount<TAccountUser>
        : TAccountUser,
      TAccountHolderGlobalState extends string
        ? ReadonlyAccount<TAccountHolderGlobalState>
        : TAccountHolderGlobalState,
      TAccountUserHolderState extends string
        ? WritableAccount<TAccountUserHolderState>
        : TAccountUserHolderState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitUserHolderStateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitUserHolderStateInstructionDataArgs = {};

export function getInitUserHolderStateInstructionDataEncoder(): FixedSizeEncoder<InitUserHolderStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INIT_USER_HOLDER_STATE_DISCRIMINATOR,
    }),
  );
}

export function getInitUserHolderStateInstructionDataDecoder(): FixedSizeDecoder<InitUserHolderStateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitUserHolderStateInstructionDataCodec(): FixedSizeCodec<
  InitUserHolderStateInstructionDataArgs,
  InitUserHolderStateInstructionData
> {
  return combineCodec(
    getInitUserHolderStateInstructionDataEncoder(),
    getInitUserHolderStateInstructionDataDecoder(),
  );
}

export type InitUserHolderStateAsyncInput<
  TAccountPayer extends string = string,
  TAccountUser extends string = string,
  TAccountHolderGlobalState extends string = string,
  TAccountUserHolderState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Payer for account creation */
  payer: TransactionSigner<TAccountPayer>;
  /** User this state is for (does not need to sign) */
  user: Address<TAccountUser>;
  /** Global holder state (read current index for initialization) */
  holderGlobalState: Address<TAccountHolderGlobalState>;
  /**
   * User holder state (initialized)
   * PDA: [b"holder_user", user]
   */
  userHolderState?: Address<TAccountUserHolderState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitUserHolderStateInstructionAsync<
  TAccountPayer extends string,
  TAccountUser extends string,
  TAccountHolderGlobalState extends string,
  TAccountUserHolderState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitUserHolderStateAsyncInput<
    TAccountPayer,
    TAccountUser,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitUserHolderStateInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUser,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    holderGlobalState: {
      value: input.holderGlobalState ?? null,
      isWritable: false,
    },
    userHolderState: { value: input.userHolderState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.userHolderState.value) {
    accounts.userHolderState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            104, 111, 108, 100, 101, 114, 95, 117, 115, 101, 114,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("user", accounts.user.value),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("user", accounts.user),
      getAccountMeta("holderGlobalState", accounts.holderGlobalState),
      getAccountMeta("userHolderState", accounts.userHolderState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitUserHolderStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitUserHolderStateInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUser,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountSystemProgram
  >);
}

export type InitUserHolderStateInput<
  TAccountPayer extends string = string,
  TAccountUser extends string = string,
  TAccountHolderGlobalState extends string = string,
  TAccountUserHolderState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Payer for account creation */
  payer: TransactionSigner<TAccountPayer>;
  /** User this state is for (does not need to sign) */
  user: Address<TAccountUser>;
  /** Global holder state (read current index for initialization) */
  holderGlobalState: Address<TAccountHolderGlobalState>;
  /**
   * User holder state (initialized)
   * PDA: [b"holder_user", user]
   */
  userHolderState: Address<TAccountUserHolderState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitUserHolderStateInstruction<
  TAccountPayer extends string,
  TAccountUser extends string,
  TAccountHolderGlobalState extends string,
  TAccountUserHolderState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitUserHolderStateInput<
    TAccountPayer,
    TAccountUser,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitUserHolderStateInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountUser,
  TAccountHolderGlobalState,
  TAccountUserHolderState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    holderGlobalState: {
      value: input.holderGlobalState ?? null,
      isWritable: false,
    },
    userHolderState: { value: input.userHolderState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("user", accounts.user),
      getAccountMeta("holderGlobalState", accounts.holderGlobalState),
      getAccountMeta("userHolderState", accounts.userHolderState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitUserHolderStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitUserHolderStateInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountUser,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountSystemProgram
  >);
}

export type ParsedInitUserHolderStateInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Payer for account creation */
    payer: TAccountMetas[0];
    /** User this state is for (does not need to sign) */
    user: TAccountMetas[1];
    /** Global holder state (read current index for initialization) */
    holderGlobalState: TAccountMetas[2];
    /**
     * User holder state (initialized)
     * PDA: [b"holder_user", user]
     */
    userHolderState: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitUserHolderStateInstructionData;
};

export function parseInitUserHolderStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitUserHolderStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      user: getNextAccount(),
      holderGlobalState: getNextAccount(),
      userHolderState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitUserHolderStateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
