/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const VERIFY_POOL_ACCOUNTING_DISCRIMINATOR = new Uint8Array([
  31, 234, 130, 0, 221, 80, 83, 220,
]);

export function getVerifyPoolAccountingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_POOL_ACCOUNTING_DISCRIMINATOR,
  );
}

export type VerifyPoolAccountingInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountBaseVault extends string | AccountMeta<string> = string,
  TAccountQuoteVault extends string | AccountMeta<string> = string,
  TAccountVerifier extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      TAccountBaseVault extends string
        ? ReadonlyAccount<TAccountBaseVault>
        : TAccountBaseVault,
      TAccountQuoteVault extends string
        ? ReadonlyAccount<TAccountQuoteVault>
        : TAccountQuoteVault,
      TAccountVerifier extends string
        ? ReadonlySignerAccount<TAccountVerifier> &
            AccountSignerMeta<TAccountVerifier>
        : TAccountVerifier,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyPoolAccountingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type VerifyPoolAccountingInstructionDataArgs = {};

export function getVerifyPoolAccountingInstructionDataEncoder(): FixedSizeEncoder<VerifyPoolAccountingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VERIFY_POOL_ACCOUNTING_DISCRIMINATOR,
    }),
  );
}

export function getVerifyPoolAccountingInstructionDataDecoder(): FixedSizeDecoder<VerifyPoolAccountingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getVerifyPoolAccountingInstructionDataCodec(): FixedSizeCodec<
  VerifyPoolAccountingInstructionDataArgs,
  VerifyPoolAccountingInstructionData
> {
  return combineCodec(
    getVerifyPoolAccountingInstructionDataEncoder(),
    getVerifyPoolAccountingInstructionDataDecoder(),
  );
}

export type VerifyPoolAccountingInput<
  TAccountPool extends string = string,
  TAccountBaseVault extends string = string,
  TAccountQuoteVault extends string = string,
  TAccountVerifier extends string = string,
> = {
  /** Pool to verify (immutable - read-only verification). */
  pool: Address<TAccountPool>;
  /** Pool's base vault (immutable - read-only verification). */
  baseVault: Address<TAccountBaseVault>;
  /** Pool's quote vault (immutable - read-only verification). */
  quoteVault: Address<TAccountQuoteVault>;
  /**
   * Admin or any authorized party that triggers verification.
   * Does not need to be admin - anyone can verify accounting.
   */
  verifier: TransactionSigner<TAccountVerifier>;
};

export function getVerifyPoolAccountingInstruction<
  TAccountPool extends string,
  TAccountBaseVault extends string,
  TAccountQuoteVault extends string,
  TAccountVerifier extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: VerifyPoolAccountingInput<
    TAccountPool,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountVerifier
  >,
  config?: { programAddress?: TProgramAddress },
): VerifyPoolAccountingInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountBaseVault,
  TAccountQuoteVault,
  TAccountVerifier
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
    baseVault: { value: input.baseVault ?? null, isWritable: false },
    quoteVault: { value: input.quoteVault ?? null, isWritable: false },
    verifier: { value: input.verifier ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("baseVault", accounts.baseVault),
      getAccountMeta("quoteVault", accounts.quoteVault),
      getAccountMeta("verifier", accounts.verifier),
    ],
    data: getVerifyPoolAccountingInstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyPoolAccountingInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountBaseVault,
    TAccountQuoteVault,
    TAccountVerifier
  >);
}

export type ParsedVerifyPoolAccountingInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool to verify (immutable - read-only verification). */
    pool: TAccountMetas[0];
    /** Pool's base vault (immutable - read-only verification). */
    baseVault: TAccountMetas[1];
    /** Pool's quote vault (immutable - read-only verification). */
    quoteVault: TAccountMetas[2];
    /**
     * Admin or any authorized party that triggers verification.
     * Does not need to be admin - anyone can verify accounting.
     */
    verifier: TAccountMetas[3];
  };
  data: VerifyPoolAccountingInstructionData;
};

export function parseVerifyPoolAccountingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyPoolAccountingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      baseVault: getNextAccount(),
      quoteVault: getNextAccount(),
      verifier: getNextAccount(),
    },
    data: getVerifyPoolAccountingInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
