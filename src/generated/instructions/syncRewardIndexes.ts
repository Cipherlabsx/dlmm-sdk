/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const SYNC_REWARD_INDEXES_DISCRIMINATOR = new Uint8Array([
  173, 205, 183, 214, 147, 35, 238, 156,
]);

export function getSyncRewardIndexesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_REWARD_INDEXES_DISCRIMINATOR,
  );
}

export type SyncRewardIndexesInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountHolderGlobalState extends string | AccountMeta<string> = string,
  TAccountNftGlobalState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountHolderGlobalState extends string
        ? WritableAccount<TAccountHolderGlobalState>
        : TAccountHolderGlobalState,
      TAccountNftGlobalState extends string
        ? WritableAccount<TAccountNftGlobalState>
        : TAccountNftGlobalState,
      ...TRemainingAccounts,
    ]
  >;

export type SyncRewardIndexesInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SyncRewardIndexesInstructionDataArgs = {};

export function getSyncRewardIndexesInstructionDataEncoder(): FixedSizeEncoder<SyncRewardIndexesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SYNC_REWARD_INDEXES_DISCRIMINATOR }),
  );
}

export function getSyncRewardIndexesInstructionDataDecoder(): FixedSizeDecoder<SyncRewardIndexesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSyncRewardIndexesInstructionDataCodec(): FixedSizeCodec<
  SyncRewardIndexesInstructionDataArgs,
  SyncRewardIndexesInstructionData
> {
  return combineCodec(
    getSyncRewardIndexesInstructionDataEncoder(),
    getSyncRewardIndexesInstructionDataDecoder(),
  );
}

export type SyncRewardIndexesAsyncInput<
  TAccountPool extends string = string,
  TAccountHolderGlobalState extends string = string,
  TAccountNftGlobalState extends string = string,
> = {
  /** Pool account (source of truth for indexes + sync checkpoints) */
  pool: Address<TAccountPool>;
  /**
   * Holder global state (PDA, receives holder index from pool)
   * PDA: [b"holder_global"]
   */
  holderGlobalState?: Address<TAccountHolderGlobalState>;
  /**
   * NFT global state (PDA, receives NFT index from pool)
   * PDA: [b"nft_global"]
   */
  nftGlobalState?: Address<TAccountNftGlobalState>;
};

export async function getSyncRewardIndexesInstructionAsync<
  TAccountPool extends string,
  TAccountHolderGlobalState extends string,
  TAccountNftGlobalState extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: SyncRewardIndexesAsyncInput<
    TAccountPool,
    TAccountHolderGlobalState,
    TAccountNftGlobalState
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SyncRewardIndexesInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountHolderGlobalState,
    TAccountNftGlobalState
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    holderGlobalState: {
      value: input.holderGlobalState ?? null,
      isWritable: true,
    },
    nftGlobalState: { value: input.nftGlobalState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.holderGlobalState.value) {
    accounts.holderGlobalState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            104, 111, 108, 100, 101, 114, 95, 103, 108, 111, 98, 97, 108,
          ]),
        ),
      ],
    });
  }
  if (!accounts.nftGlobalState.value) {
    accounts.nftGlobalState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([110, 102, 116, 95, 103, 108, 111, 98, 97, 108]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("holderGlobalState", accounts.holderGlobalState),
      getAccountMeta("nftGlobalState", accounts.nftGlobalState),
    ],
    data: getSyncRewardIndexesInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncRewardIndexesInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountHolderGlobalState,
    TAccountNftGlobalState
  >);
}

export type SyncRewardIndexesInput<
  TAccountPool extends string = string,
  TAccountHolderGlobalState extends string = string,
  TAccountNftGlobalState extends string = string,
> = {
  /** Pool account (source of truth for indexes + sync checkpoints) */
  pool: Address<TAccountPool>;
  /**
   * Holder global state (PDA, receives holder index from pool)
   * PDA: [b"holder_global"]
   */
  holderGlobalState: Address<TAccountHolderGlobalState>;
  /**
   * NFT global state (PDA, receives NFT index from pool)
   * PDA: [b"nft_global"]
   */
  nftGlobalState: Address<TAccountNftGlobalState>;
};

export function getSyncRewardIndexesInstruction<
  TAccountPool extends string,
  TAccountHolderGlobalState extends string,
  TAccountNftGlobalState extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: SyncRewardIndexesInput<
    TAccountPool,
    TAccountHolderGlobalState,
    TAccountNftGlobalState
  >,
  config?: { programAddress?: TProgramAddress },
): SyncRewardIndexesInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountHolderGlobalState,
  TAccountNftGlobalState
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    holderGlobalState: {
      value: input.holderGlobalState ?? null,
      isWritable: true,
    },
    nftGlobalState: { value: input.nftGlobalState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("holderGlobalState", accounts.holderGlobalState),
      getAccountMeta("nftGlobalState", accounts.nftGlobalState),
    ],
    data: getSyncRewardIndexesInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncRewardIndexesInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountHolderGlobalState,
    TAccountNftGlobalState
  >);
}

export type ParsedSyncRewardIndexesInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool account (source of truth for indexes + sync checkpoints) */
    pool: TAccountMetas[0];
    /**
     * Holder global state (PDA, receives holder index from pool)
     * PDA: [b"holder_global"]
     */
    holderGlobalState: TAccountMetas[1];
    /**
     * NFT global state (PDA, receives NFT index from pool)
     * PDA: [b"nft_global"]
     */
    nftGlobalState: TAccountMetas[2];
  };
  data: SyncRewardIndexesInstructionData;
};

export function parseSyncRewardIndexesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncRewardIndexesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      holderGlobalState: getNextAccount(),
      nftGlobalState: getNextAccount(),
    },
    data: getSyncRewardIndexesInstructionDataDecoder().decode(instruction.data),
  };
}
