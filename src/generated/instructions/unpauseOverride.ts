/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const UNPAUSE_OVERRIDE_DISCRIMINATOR = new Uint8Array([
  150, 175, 134, 15, 132, 92, 237, 185,
]);

export function getUnpauseOverrideDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNPAUSE_OVERRIDE_DISCRIMINATOR,
  );
}

export type UnpauseOverrideInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountSquadsSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountSquadsSigner extends string
        ? ReadonlySignerAccount<TAccountSquadsSigner> &
            AccountSignerMeta<TAccountSquadsSigner>
        : TAccountSquadsSigner,
      ...TRemainingAccounts,
    ]
  >;

export type UnpauseOverrideInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UnpauseOverrideInstructionDataArgs = {};

export function getUnpauseOverrideInstructionDataEncoder(): FixedSizeEncoder<UnpauseOverrideInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UNPAUSE_OVERRIDE_DISCRIMINATOR }),
  );
}

export function getUnpauseOverrideInstructionDataDecoder(): FixedSizeDecoder<UnpauseOverrideInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnpauseOverrideInstructionDataCodec(): FixedSizeCodec<
  UnpauseOverrideInstructionDataArgs,
  UnpauseOverrideInstructionData
> {
  return combineCodec(
    getUnpauseOverrideInstructionDataEncoder(),
    getUnpauseOverrideInstructionDataDecoder(),
  );
}

export type UnpauseOverrideInput<
  TAccountPool extends string = string,
  TAccountSquadsSigner extends string = string,
> = {
  pool: Address<TAccountPool>;
  /**
   * Squads multisig wallet signer (hardcoded address).
   * This instruction is authorized only when this exact wallet signs.
   */
  squadsSigner: TransactionSigner<TAccountSquadsSigner>;
};

export function getUnpauseOverrideInstruction<
  TAccountPool extends string,
  TAccountSquadsSigner extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: UnpauseOverrideInput<TAccountPool, TAccountSquadsSigner>,
  config?: { programAddress?: TProgramAddress },
): UnpauseOverrideInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountSquadsSigner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    squadsSigner: { value: input.squadsSigner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("squadsSigner", accounts.squadsSigner),
    ],
    data: getUnpauseOverrideInstructionDataEncoder().encode({}),
    programAddress,
  } as UnpauseOverrideInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountSquadsSigner
  >);
}

export type ParsedUnpauseOverrideInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /**
     * Squads multisig wallet signer (hardcoded address).
     * This instruction is authorized only when this exact wallet signs.
     */
    squadsSigner: TAccountMetas[1];
  };
  data: UnpauseOverrideInstructionData;
};

export function parseUnpauseOverrideInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnpauseOverrideInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pool: getNextAccount(), squadsSigner: getNextAccount() },
    data: getUnpauseOverrideInstructionDataDecoder().decode(instruction.data),
  };
}
