/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const SET_PAUSE_BITS_DISCRIMINATOR = new Uint8Array([
  122, 45, 85, 156, 176, 64, 45, 83,
]);

export function getSetPauseBitsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_PAUSE_BITS_DISCRIMINATOR,
  );
}

export type SetPauseBitsInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountPauseGuardian extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountPauseGuardian extends string
        ? ReadonlySignerAccount<TAccountPauseGuardian> &
            AccountSignerMeta<TAccountPauseGuardian>
        : TAccountPauseGuardian,
      ...TRemainingAccounts,
    ]
  >;

export type SetPauseBitsInstructionData = {
  discriminator: ReadonlyUint8Array;
  newBits: number;
};

export type SetPauseBitsInstructionDataArgs = { newBits: number };

export function getSetPauseBitsInstructionDataEncoder(): FixedSizeEncoder<SetPauseBitsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newBits", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_PAUSE_BITS_DISCRIMINATOR }),
  );
}

export function getSetPauseBitsInstructionDataDecoder(): FixedSizeDecoder<SetPauseBitsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newBits", getU8Decoder()],
  ]);
}

export function getSetPauseBitsInstructionDataCodec(): FixedSizeCodec<
  SetPauseBitsInstructionDataArgs,
  SetPauseBitsInstructionData
> {
  return combineCodec(
    getSetPauseBitsInstructionDataEncoder(),
    getSetPauseBitsInstructionDataDecoder(),
  );
}

export type SetPauseBitsInput<
  TAccountPool extends string = string,
  TAccountPauseGuardian extends string = string,
> = {
  pool: Address<TAccountPool>;
  pauseGuardian: TransactionSigner<TAccountPauseGuardian>;
  newBits: SetPauseBitsInstructionDataArgs["newBits"];
};

export function getSetPauseBitsInstruction<
  TAccountPool extends string,
  TAccountPauseGuardian extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: SetPauseBitsInput<TAccountPool, TAccountPauseGuardian>,
  config?: { programAddress?: TProgramAddress },
): SetPauseBitsInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountPauseGuardian
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    pauseGuardian: { value: input.pauseGuardian ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("pauseGuardian", accounts.pauseGuardian),
    ],
    data: getSetPauseBitsInstructionDataEncoder().encode(
      args as SetPauseBitsInstructionDataArgs,
    ),
    programAddress,
  } as SetPauseBitsInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountPauseGuardian
  >);
}

export type ParsedSetPauseBitsInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    pauseGuardian: TAccountMetas[1];
  };
  data: SetPauseBitsInstructionData;
};

export function parseSetPauseBitsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPauseBitsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pool: getNextAccount(), pauseGuardian: getNextAccount() },
    data: getSetPauseBitsInstructionDataDecoder().decode(instruction.data),
  };
}
