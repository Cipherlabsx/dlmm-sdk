/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI32Decoder,
  getI32Encoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const CREATE_BIN_ARRAY_DISCRIMINATOR = new Uint8Array([
  107, 26, 23, 62, 137, 213, 131, 235,
]);

export function getCreateBinArrayDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_BIN_ARRAY_DISCRIMINATOR,
  );
}

export type CreateBinArrayInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountBinArray extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountBinArray extends string
        ? WritableAccount<TAccountBinArray>
        : TAccountBinArray,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateBinArrayInstructionData = {
  discriminator: ReadonlyUint8Array;
  lowerBinIndex: number;
};

export type CreateBinArrayInstructionDataArgs = { lowerBinIndex: number };

export function getCreateBinArrayInstructionDataEncoder(): FixedSizeEncoder<CreateBinArrayInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["lowerBinIndex", getI32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_BIN_ARRAY_DISCRIMINATOR }),
  );
}

export function getCreateBinArrayInstructionDataDecoder(): FixedSizeDecoder<CreateBinArrayInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["lowerBinIndex", getI32Decoder()],
  ]);
}

export function getCreateBinArrayInstructionDataCodec(): FixedSizeCodec<
  CreateBinArrayInstructionDataArgs,
  CreateBinArrayInstructionData
> {
  return combineCodec(
    getCreateBinArrayInstructionDataEncoder(),
    getCreateBinArrayInstructionDataDecoder(),
  );
}

export type CreateBinArrayAsyncInput<
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
  TAccountBinArray extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pool that owns this bin array. */
  pool: Address<TAccountPool>;
  /** Pool admin (must sign to create bins). */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * New BinArray account to initialize.
   * Seeds: ["bin_array", pool, lower_bin_index_le]
   * lower_bin_index must be aligned to 64-bin boundaries (0, 64, 128, -64, etc.)
   */
  binArray?: Address<TAccountBinArray>;
  systemProgram?: Address<TAccountSystemProgram>;
  lowerBinIndex: CreateBinArrayInstructionDataArgs["lowerBinIndex"];
};

export async function getCreateBinArrayInstructionAsync<
  TAccountPool extends string,
  TAccountAdmin extends string,
  TAccountBinArray extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: CreateBinArrayAsyncInput<
    TAccountPool,
    TAccountAdmin,
    TAccountBinArray,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateBinArrayInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin,
    TAccountBinArray,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    binArray: { value: input.binArray ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.binArray.value) {
    accounts.binArray.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([98, 105, 110, 95, 97, 114, 114, 97, 121]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("pool", accounts.pool.value),
        ),
        getI32Encoder().encode(
          getNonNullResolvedInstructionInput(
            "lowerBinIndex",
            args.lowerBinIndex,
          ),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("binArray", accounts.binArray),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateBinArrayInstructionDataEncoder().encode(
      args as CreateBinArrayInstructionDataArgs,
    ),
    programAddress,
  } as CreateBinArrayInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin,
    TAccountBinArray,
    TAccountSystemProgram
  >);
}

export type CreateBinArrayInput<
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
  TAccountBinArray extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pool that owns this bin array. */
  pool: Address<TAccountPool>;
  /** Pool admin (must sign to create bins). */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * New BinArray account to initialize.
   * Seeds: ["bin_array", pool, lower_bin_index_le]
   * lower_bin_index must be aligned to 64-bin boundaries (0, 64, 128, -64, etc.)
   */
  binArray: Address<TAccountBinArray>;
  systemProgram?: Address<TAccountSystemProgram>;
  lowerBinIndex: CreateBinArrayInstructionDataArgs["lowerBinIndex"];
};

export function getCreateBinArrayInstruction<
  TAccountPool extends string,
  TAccountAdmin extends string,
  TAccountBinArray extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: CreateBinArrayInput<
    TAccountPool,
    TAccountAdmin,
    TAccountBinArray,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateBinArrayInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountAdmin,
  TAccountBinArray,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    binArray: { value: input.binArray ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("binArray", accounts.binArray),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateBinArrayInstructionDataEncoder().encode(
      args as CreateBinArrayInstructionDataArgs,
    ),
    programAddress,
  } as CreateBinArrayInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin,
    TAccountBinArray,
    TAccountSystemProgram
  >);
}

export type ParsedCreateBinArrayInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool that owns this bin array. */
    pool: TAccountMetas[0];
    /** Pool admin (must sign to create bins). */
    admin: TAccountMetas[1];
    /**
     * New BinArray account to initialize.
     * Seeds: ["bin_array", pool, lower_bin_index_le]
     * lower_bin_index must be aligned to 64-bin boundaries (0, 64, 128, -64, etc.)
     */
    binArray: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateBinArrayInstructionData;
};

export function parseCreateBinArrayInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateBinArrayInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      admin: getNextAccount(),
      binArray: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateBinArrayInstructionDataDecoder().decode(instruction.data),
  };
}
