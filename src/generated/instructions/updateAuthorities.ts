/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_AUTHORITIES_DISCRIMINATOR = new Uint8Array([
  175, 228, 137, 18, 175, 70, 220, 165,
]);

export function getUpdateAuthoritiesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_AUTHORITIES_DISCRIMINATOR,
  );
}

export type UpdateAuthoritiesInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAuthoritiesInstructionData = {
  discriminator: ReadonlyUint8Array;
  configAuthority: Address;
  pauseGuardian: Address;
  feeWithdrawAuthority: Address;
};

export type UpdateAuthoritiesInstructionDataArgs = {
  configAuthority: Address;
  pauseGuardian: Address;
  feeWithdrawAuthority: Address;
};

export function getUpdateAuthoritiesInstructionDataEncoder(): FixedSizeEncoder<UpdateAuthoritiesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["configAuthority", getAddressEncoder()],
      ["pauseGuardian", getAddressEncoder()],
      ["feeWithdrawAuthority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_AUTHORITIES_DISCRIMINATOR }),
  );
}

export function getUpdateAuthoritiesInstructionDataDecoder(): FixedSizeDecoder<UpdateAuthoritiesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["configAuthority", getAddressDecoder()],
    ["pauseGuardian", getAddressDecoder()],
    ["feeWithdrawAuthority", getAddressDecoder()],
  ]);
}

export function getUpdateAuthoritiesInstructionDataCodec(): FixedSizeCodec<
  UpdateAuthoritiesInstructionDataArgs,
  UpdateAuthoritiesInstructionData
> {
  return combineCodec(
    getUpdateAuthoritiesInstructionDataEncoder(),
    getUpdateAuthoritiesInstructionDataDecoder(),
  );
}

export type UpdateAuthoritiesInput<
  TAccountPool extends string = string,
  TAccountAdmin extends string = string,
> = {
  /** Admin validation via has_one (AccountLoader validates discriminator + program ownership) */
  pool: Address<TAccountPool>;
  /** Admin signer (must match pool.admin) */
  admin: TransactionSigner<TAccountAdmin>;
  configAuthority: UpdateAuthoritiesInstructionDataArgs["configAuthority"];
  pauseGuardian: UpdateAuthoritiesInstructionDataArgs["pauseGuardian"];
  feeWithdrawAuthority: UpdateAuthoritiesInstructionDataArgs["feeWithdrawAuthority"];
};

export function getUpdateAuthoritiesInstruction<
  TAccountPool extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: UpdateAuthoritiesInput<TAccountPool, TAccountAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdateAuthoritiesInstruction<TProgramAddress, TAccountPool, TAccountAdmin> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("admin", accounts.admin),
    ],
    data: getUpdateAuthoritiesInstructionDataEncoder().encode(
      args as UpdateAuthoritiesInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAuthoritiesInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountAdmin
  >);
}

export type ParsedUpdateAuthoritiesInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Admin validation via has_one (AccountLoader validates discriminator + program ownership) */
    pool: TAccountMetas[0];
    /** Admin signer (must match pool.admin) */
    admin: TAccountMetas[1];
  };
  data: UpdateAuthoritiesInstructionData;
};

export function parseUpdateAuthoritiesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateAuthoritiesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { pool: getNextAccount(), admin: getNextAccount() },
    data: getUpdateAuthoritiesInstructionDataDecoder().decode(instruction.data),
  };
}
