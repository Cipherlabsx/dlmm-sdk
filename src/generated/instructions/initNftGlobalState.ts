/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const INIT_NFT_GLOBAL_STATE_DISCRIMINATOR = new Uint8Array([
  126, 182, 160, 21, 28, 63, 16, 75,
]);

export function getInitNftGlobalStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_NFT_GLOBAL_STATE_DISCRIMINATOR,
  );
}

export type InitNftGlobalStateInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountNftGlobalState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountNftGlobalState extends string
        ? WritableAccount<TAccountNftGlobalState>
        : TAccountNftGlobalState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitNftGlobalStateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitNftGlobalStateInstructionDataArgs = {};

export function getInitNftGlobalStateInstructionDataEncoder(): FixedSizeEncoder<InitNftGlobalStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INIT_NFT_GLOBAL_STATE_DISCRIMINATOR,
    }),
  );
}

export function getInitNftGlobalStateInstructionDataDecoder(): FixedSizeDecoder<InitNftGlobalStateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitNftGlobalStateInstructionDataCodec(): FixedSizeCodec<
  InitNftGlobalStateInstructionDataArgs,
  InitNftGlobalStateInstructionData
> {
  return combineCodec(
    getInitNftGlobalStateInstructionDataEncoder(),
    getInitNftGlobalStateInstructionDataDecoder(),
  );
}

export type InitNftGlobalStateAsyncInput<
  TAccountAdmin extends string = string,
  TAccountNftGlobalState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Signer paying rent and performing the initialization.
   * Must match PROTOCOL_ADMIN exactly.
   */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * Global NFT state PDA.
   * Created once and shared across all pools.
   */
  nftGlobalState?: Address<TAccountNftGlobalState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitNftGlobalStateInstructionAsync<
  TAccountAdmin extends string,
  TAccountNftGlobalState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitNftGlobalStateAsyncInput<
    TAccountAdmin,
    TAccountNftGlobalState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitNftGlobalStateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountNftGlobalState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    nftGlobalState: { value: input.nftGlobalState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.nftGlobalState.value) {
    accounts.nftGlobalState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([110, 102, 116, 95, 103, 108, 111, 98, 97, 108]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("nftGlobalState", accounts.nftGlobalState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitNftGlobalStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitNftGlobalStateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountNftGlobalState,
    TAccountSystemProgram
  >);
}

export type InitNftGlobalStateInput<
  TAccountAdmin extends string = string,
  TAccountNftGlobalState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Signer paying rent and performing the initialization.
   * Must match PROTOCOL_ADMIN exactly.
   */
  admin: TransactionSigner<TAccountAdmin>;
  /**
   * Global NFT state PDA.
   * Created once and shared across all pools.
   */
  nftGlobalState: Address<TAccountNftGlobalState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitNftGlobalStateInstruction<
  TAccountAdmin extends string,
  TAccountNftGlobalState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: InitNftGlobalStateInput<
    TAccountAdmin,
    TAccountNftGlobalState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitNftGlobalStateInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountNftGlobalState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    nftGlobalState: { value: input.nftGlobalState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("nftGlobalState", accounts.nftGlobalState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitNftGlobalStateInstructionDataEncoder().encode({}),
    programAddress,
  } as InitNftGlobalStateInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountNftGlobalState,
    TAccountSystemProgram
  >);
}

export type ParsedInitNftGlobalStateInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Signer paying rent and performing the initialization.
     * Must match PROTOCOL_ADMIN exactly.
     */
    admin: TAccountMetas[0];
    /**
     * Global NFT state PDA.
     * Created once and shared across all pools.
     */
    nftGlobalState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitNftGlobalStateInstructionData;
};

export function parseInitNftGlobalStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitNftGlobalStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      nftGlobalState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitNftGlobalStateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
