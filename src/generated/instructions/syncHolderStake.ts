/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const SYNC_HOLDER_STAKE_DISCRIMINATOR = new Uint8Array([
  151, 230, 186, 138, 237, 187, 231, 155,
]);

export function getSyncHolderStakeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_HOLDER_STAKE_DISCRIMINATOR,
  );
}

export type SyncHolderStakeInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountHolderGlobalState extends string | AccountMeta<string> = string,
  TAccountUserHolderState extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountStreamflowStakeEntry extends string | AccountMeta<string> = string,
  TAccountStreamflowProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHolderGlobalState extends string
        ? ReadonlyAccount<TAccountHolderGlobalState>
        : TAccountHolderGlobalState,
      TAccountUserHolderState extends string
        ? WritableAccount<TAccountUserHolderState>
        : TAccountUserHolderState,
      TAccountUser extends string
        ? ReadonlySignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountStreamflowStakeEntry extends string
        ? ReadonlyAccount<TAccountStreamflowStakeEntry>
        : TAccountStreamflowStakeEntry,
      TAccountStreamflowProgram extends string
        ? ReadonlyAccount<TAccountStreamflowProgram>
        : TAccountStreamflowProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SyncHolderStakeInstructionData = {
  discriminator: ReadonlyUint8Array;
  stakePoolAddress: Address;
  stakeEntryNonce: number;
};

export type SyncHolderStakeInstructionDataArgs = {
  stakePoolAddress: Address;
  stakeEntryNonce: number;
};

export function getSyncHolderStakeInstructionDataEncoder(): FixedSizeEncoder<SyncHolderStakeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stakePoolAddress", getAddressEncoder()],
      ["stakeEntryNonce", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SYNC_HOLDER_STAKE_DISCRIMINATOR }),
  );
}

export function getSyncHolderStakeInstructionDataDecoder(): FixedSizeDecoder<SyncHolderStakeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stakePoolAddress", getAddressDecoder()],
    ["stakeEntryNonce", getU32Decoder()],
  ]);
}

export function getSyncHolderStakeInstructionDataCodec(): FixedSizeCodec<
  SyncHolderStakeInstructionDataArgs,
  SyncHolderStakeInstructionData
> {
  return combineCodec(
    getSyncHolderStakeInstructionDataEncoder(),
    getSyncHolderStakeInstructionDataDecoder(),
  );
}

export type SyncHolderStakeAsyncInput<
  TAccountHolderGlobalState extends string = string,
  TAccountUserHolderState extends string = string,
  TAccountUser extends string = string,
  TAccountStreamflowStakeEntry extends string = string,
  TAccountStreamflowProgram extends string = string,
> = {
  /**
   * Global holder reward state (for current index)
   * READ AS RAW BYTES to avoid zero-copy AccountLoader heap usage.
   * Two zero-copy loads (global + user) exceed the 256KB heap limit.
   */
  holderGlobalState: Address<TAccountHolderGlobalState>;
  /**
   * User's holder state (checkpoint tracking)
   * MUST be pre-initialized via init_user_holder_state instruction
   * PDA: [b"holder_user", user]
   */
  userHolderState?: Address<TAccountUserHolderState>;
  /** User (signer) */
  user: TransactionSigner<TAccountUser>;
  /** SECURITY FIX: Streamflow StakeEntry account for verification */
  streamflowStakeEntry: Address<TAccountStreamflowStakeEntry>;
  /** SECURITY FIX: Streamflow program for verification */
  streamflowProgram: Address<TAccountStreamflowProgram>;
  stakePoolAddress: SyncHolderStakeInstructionDataArgs["stakePoolAddress"];
  stakeEntryNonce: SyncHolderStakeInstructionDataArgs["stakeEntryNonce"];
};

export async function getSyncHolderStakeInstructionAsync<
  TAccountHolderGlobalState extends string,
  TAccountUserHolderState extends string,
  TAccountUser extends string,
  TAccountStreamflowStakeEntry extends string,
  TAccountStreamflowProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: SyncHolderStakeAsyncInput<
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountUser,
    TAccountStreamflowStakeEntry,
    TAccountStreamflowProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SyncHolderStakeInstruction<
    TProgramAddress,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountUser,
    TAccountStreamflowStakeEntry,
    TAccountStreamflowProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    holderGlobalState: {
      value: input.holderGlobalState ?? null,
      isWritable: false,
    },
    userHolderState: { value: input.userHolderState ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    streamflowStakeEntry: {
      value: input.streamflowStakeEntry ?? null,
      isWritable: false,
    },
    streamflowProgram: {
      value: input.streamflowProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userHolderState.value) {
    accounts.userHolderState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            104, 111, 108, 100, 101, 114, 95, 117, 115, 101, 114,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("user", accounts.user.value),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("holderGlobalState", accounts.holderGlobalState),
      getAccountMeta("userHolderState", accounts.userHolderState),
      getAccountMeta("user", accounts.user),
      getAccountMeta("streamflowStakeEntry", accounts.streamflowStakeEntry),
      getAccountMeta("streamflowProgram", accounts.streamflowProgram),
    ],
    data: getSyncHolderStakeInstructionDataEncoder().encode(
      args as SyncHolderStakeInstructionDataArgs,
    ),
    programAddress,
  } as SyncHolderStakeInstruction<
    TProgramAddress,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountUser,
    TAccountStreamflowStakeEntry,
    TAccountStreamflowProgram
  >);
}

export type SyncHolderStakeInput<
  TAccountHolderGlobalState extends string = string,
  TAccountUserHolderState extends string = string,
  TAccountUser extends string = string,
  TAccountStreamflowStakeEntry extends string = string,
  TAccountStreamflowProgram extends string = string,
> = {
  /**
   * Global holder reward state (for current index)
   * READ AS RAW BYTES to avoid zero-copy AccountLoader heap usage.
   * Two zero-copy loads (global + user) exceed the 256KB heap limit.
   */
  holderGlobalState: Address<TAccountHolderGlobalState>;
  /**
   * User's holder state (checkpoint tracking)
   * MUST be pre-initialized via init_user_holder_state instruction
   * PDA: [b"holder_user", user]
   */
  userHolderState: Address<TAccountUserHolderState>;
  /** User (signer) */
  user: TransactionSigner<TAccountUser>;
  /** SECURITY FIX: Streamflow StakeEntry account for verification */
  streamflowStakeEntry: Address<TAccountStreamflowStakeEntry>;
  /** SECURITY FIX: Streamflow program for verification */
  streamflowProgram: Address<TAccountStreamflowProgram>;
  stakePoolAddress: SyncHolderStakeInstructionDataArgs["stakePoolAddress"];
  stakeEntryNonce: SyncHolderStakeInstructionDataArgs["stakeEntryNonce"];
};

export function getSyncHolderStakeInstruction<
  TAccountHolderGlobalState extends string,
  TAccountUserHolderState extends string,
  TAccountUser extends string,
  TAccountStreamflowStakeEntry extends string,
  TAccountStreamflowProgram extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: SyncHolderStakeInput<
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountUser,
    TAccountStreamflowStakeEntry,
    TAccountStreamflowProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SyncHolderStakeInstruction<
  TProgramAddress,
  TAccountHolderGlobalState,
  TAccountUserHolderState,
  TAccountUser,
  TAccountStreamflowStakeEntry,
  TAccountStreamflowProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    holderGlobalState: {
      value: input.holderGlobalState ?? null,
      isWritable: false,
    },
    userHolderState: { value: input.userHolderState ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: false },
    streamflowStakeEntry: {
      value: input.streamflowStakeEntry ?? null,
      isWritable: false,
    },
    streamflowProgram: {
      value: input.streamflowProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("holderGlobalState", accounts.holderGlobalState),
      getAccountMeta("userHolderState", accounts.userHolderState),
      getAccountMeta("user", accounts.user),
      getAccountMeta("streamflowStakeEntry", accounts.streamflowStakeEntry),
      getAccountMeta("streamflowProgram", accounts.streamflowProgram),
    ],
    data: getSyncHolderStakeInstructionDataEncoder().encode(
      args as SyncHolderStakeInstructionDataArgs,
    ),
    programAddress,
  } as SyncHolderStakeInstruction<
    TProgramAddress,
    TAccountHolderGlobalState,
    TAccountUserHolderState,
    TAccountUser,
    TAccountStreamflowStakeEntry,
    TAccountStreamflowProgram
  >);
}

export type ParsedSyncHolderStakeInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Global holder reward state (for current index)
     * READ AS RAW BYTES to avoid zero-copy AccountLoader heap usage.
     * Two zero-copy loads (global + user) exceed the 256KB heap limit.
     */
    holderGlobalState: TAccountMetas[0];
    /**
     * User's holder state (checkpoint tracking)
     * MUST be pre-initialized via init_user_holder_state instruction
     * PDA: [b"holder_user", user]
     */
    userHolderState: TAccountMetas[1];
    /** User (signer) */
    user: TAccountMetas[2];
    /** SECURITY FIX: Streamflow StakeEntry account for verification */
    streamflowStakeEntry: TAccountMetas[3];
    /** SECURITY FIX: Streamflow program for verification */
    streamflowProgram: TAccountMetas[4];
  };
  data: SyncHolderStakeInstructionData;
};

export function parseSyncHolderStakeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncHolderStakeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      holderGlobalState: getNextAccount(),
      userHolderState: getNextAccount(),
      user: getNextAccount(),
      streamflowStakeEntry: getNextAccount(),
      streamflowProgram: getNextAccount(),
    },
    data: getSyncHolderStakeInstructionDataDecoder().decode(instruction.data),
  };
}
