/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { ORBIT_FINANCE_PROGRAM_ADDRESS } from "../programs";

export const VIEW_FARMING_POSITION_DISCRIMINATOR = new Uint8Array([
  29, 39, 65, 136, 187, 153, 243, 130,
]);

export function getViewFarmingPositionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VIEW_FARMING_POSITION_DISCRIMINATOR,
  );
}

export type ViewFarmingPositionInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountPositionBin extends string | AccountMeta<string> = string,
  TAccountBinArray extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      TAccountPosition extends string
        ? ReadonlyAccount<TAccountPosition>
        : TAccountPosition,
      TAccountPositionBin extends string
        ? ReadonlyAccount<TAccountPositionBin>
        : TAccountPositionBin,
      TAccountBinArray extends string
        ? ReadonlyAccount<TAccountBinArray>
        : TAccountBinArray,
      ...TRemainingAccounts,
    ]
  >;

export type ViewFarmingPositionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ViewFarmingPositionInstructionDataArgs = {};

export function getViewFarmingPositionInstructionDataEncoder(): FixedSizeEncoder<ViewFarmingPositionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VIEW_FARMING_POSITION_DISCRIMINATOR,
    }),
  );
}

export function getViewFarmingPositionInstructionDataDecoder(): FixedSizeDecoder<ViewFarmingPositionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getViewFarmingPositionInstructionDataCodec(): FixedSizeCodec<
  ViewFarmingPositionInstructionDataArgs,
  ViewFarmingPositionInstructionData
> {
  return combineCodec(
    getViewFarmingPositionInstructionDataEncoder(),
    getViewFarmingPositionInstructionDataDecoder(),
  );
}

export type ViewFarmingPositionInput<
  TAccountPool extends string = string,
  TAccountPosition extends string = string,
  TAccountPositionBin extends string = string,
  TAccountBinArray extends string = string,
> = {
  /** Pool account (validation only) */
  pool: Address<TAccountPool>;
  /** Position account */
  position: Address<TAccountPosition>;
  /** PositionBin account */
  positionBin: Address<TAccountPositionBin>;
  /** BinArray containing the bin */
  binArray: Address<TAccountBinArray>;
};

export function getViewFarmingPositionInstruction<
  TAccountPool extends string,
  TAccountPosition extends string,
  TAccountPositionBin extends string,
  TAccountBinArray extends string,
  TProgramAddress extends Address = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
>(
  input: ViewFarmingPositionInput<
    TAccountPool,
    TAccountPosition,
    TAccountPositionBin,
    TAccountBinArray
  >,
  config?: { programAddress?: TProgramAddress },
): ViewFarmingPositionInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountPosition,
  TAccountPositionBin,
  TAccountBinArray
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORBIT_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: false },
    positionBin: { value: input.positionBin ?? null, isWritable: false },
    binArray: { value: input.binArray ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("position", accounts.position),
      getAccountMeta("positionBin", accounts.positionBin),
      getAccountMeta("binArray", accounts.binArray),
    ],
    data: getViewFarmingPositionInstructionDataEncoder().encode({}),
    programAddress,
  } as ViewFarmingPositionInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountPosition,
    TAccountPositionBin,
    TAccountBinArray
  >);
}

export type ParsedViewFarmingPositionInstruction<
  TProgram extends string = typeof ORBIT_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pool account (validation only) */
    pool: TAccountMetas[0];
    /** Position account */
    position: TAccountMetas[1];
    /** PositionBin account */
    positionBin: TAccountMetas[2];
    /** BinArray containing the bin */
    binArray: TAccountMetas[3];
  };
  data: ViewFarmingPositionInstructionData;
};

export function parseViewFarmingPositionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedViewFarmingPositionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      position: getNextAccount(),
      positionBin: getNextAccount(),
      binArray: getNextAccount(),
    },
    data: getViewFarmingPositionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
