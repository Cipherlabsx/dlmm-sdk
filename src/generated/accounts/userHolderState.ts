/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const USER_HOLDER_STATE_DISCRIMINATOR = new Uint8Array([
  109, 105, 38, 190, 82, 186, 180, 81,
]);

export function getUserHolderStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    USER_HOLDER_STATE_DISCRIMINATOR,
  );
}

export type UserHolderState = {
  discriminator: ReadonlyUint8Array;
  /**
   * Reward index when current stake period began
   * Set on each sync to current global index
   * Used to calculate rewards accumulated during current period
   */
  stakeEntryIndexQ128: bigint;
  /**
   * Accumulated rewards from previous stake periods (unclaimed)
   * Preserved across syncs to allow multi-period accumulation
   * Reset to 0 on claim
   */
  pendingRewards: bigint;
  /**
   * Total rewards claimed by this user (cumulative, never decreases)
   * Sum across all periods and claim events
   * Denominated in quote token (e.g., USDC)
   */
  totalClaimed: bigint;
  /** User this state belongs to */
  user: Address;
  /**
   * Reserved space for future upgrades (40 bytes)
   * Reduced from 64 bytes due to new fields
   */
  reserved: Array<bigint>;
  /**
   * Current staked CIPHER amount (per last sync)
   * Updated via `sync_holder_stake()` after each Streamflow stake/unstake
   *
   * SECURITY: ON-CHAIN VERIFIED via Streamflow CPI (NOT self-reported)
   * - Reads actual amount from Streamflow StakeEntry account
   * - Verifies account owned by Streamflow program
   * - Validates authority, stake pool, nonce, and active status
   * - See: sync_holder_stake.rs verify_and_get_stake_amount()
   */
  currentStakedAmount: bigint;
  /**
   * Last timestamp when user claimed rewards
   * Used for analytics and monitoring
   */
  lastClaimTime: bigint;
  /**
   * Last timestamp when user synced their stake
   * Used to detect stale state and prompt frontend to sync
   */
  lastSyncTime: bigint;
  /** Bump seed for PDA derivation */
  bump: number;
  /** Additional padding to align struct to 16-byte boundary (Pod requirement) */
  pad: ReadonlyUint8Array;
};

export type UserHolderStateArgs = {
  /**
   * Reward index when current stake period began
   * Set on each sync to current global index
   * Used to calculate rewards accumulated during current period
   */
  stakeEntryIndexQ128: number | bigint;
  /**
   * Accumulated rewards from previous stake periods (unclaimed)
   * Preserved across syncs to allow multi-period accumulation
   * Reset to 0 on claim
   */
  pendingRewards: number | bigint;
  /**
   * Total rewards claimed by this user (cumulative, never decreases)
   * Sum across all periods and claim events
   * Denominated in quote token (e.g., USDC)
   */
  totalClaimed: number | bigint;
  /** User this state belongs to */
  user: Address;
  /**
   * Reserved space for future upgrades (40 bytes)
   * Reduced from 64 bytes due to new fields
   */
  reserved: Array<number | bigint>;
  /**
   * Current staked CIPHER amount (per last sync)
   * Updated via `sync_holder_stake()` after each Streamflow stake/unstake
   *
   * SECURITY: ON-CHAIN VERIFIED via Streamflow CPI (NOT self-reported)
   * - Reads actual amount from Streamflow StakeEntry account
   * - Verifies account owned by Streamflow program
   * - Validates authority, stake pool, nonce, and active status
   * - See: sync_holder_stake.rs verify_and_get_stake_amount()
   */
  currentStakedAmount: number | bigint;
  /**
   * Last timestamp when user claimed rewards
   * Used for analytics and monitoring
   */
  lastClaimTime: number | bigint;
  /**
   * Last timestamp when user synced their stake
   * Used to detect stale state and prompt frontend to sync
   */
  lastSyncTime: number | bigint;
  /** Bump seed for PDA derivation */
  bump: number;
  /** Additional padding to align struct to 16-byte boundary (Pod requirement) */
  pad: ReadonlyUint8Array;
};

/** Gets the encoder for {@link UserHolderStateArgs} account data. */
export function getUserHolderStateEncoder(): FixedSizeEncoder<UserHolderStateArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stakeEntryIndexQ128", getU128Encoder()],
      ["pendingRewards", getU128Encoder()],
      ["totalClaimed", getU128Encoder()],
      ["user", getAddressEncoder()],
      ["reserved", getArrayEncoder(getU64Encoder(), { size: 5 })],
      ["currentStakedAmount", getU64Encoder()],
      ["lastClaimTime", getI64Encoder()],
      ["lastSyncTime", getI64Encoder()],
      ["bump", getU8Encoder()],
      ["pad", fixEncoderSize(getBytesEncoder(), 15)],
    ]),
    (value) => ({ ...value, discriminator: USER_HOLDER_STATE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link UserHolderState} account data. */
export function getUserHolderStateDecoder(): FixedSizeDecoder<UserHolderState> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stakeEntryIndexQ128", getU128Decoder()],
    ["pendingRewards", getU128Decoder()],
    ["totalClaimed", getU128Decoder()],
    ["user", getAddressDecoder()],
    ["reserved", getArrayDecoder(getU64Decoder(), { size: 5 })],
    ["currentStakedAmount", getU64Decoder()],
    ["lastClaimTime", getI64Decoder()],
    ["lastSyncTime", getI64Decoder()],
    ["bump", getU8Decoder()],
    ["pad", fixDecoderSize(getBytesDecoder(), 15)],
  ]);
}

/** Gets the codec for {@link UserHolderState} account data. */
export function getUserHolderStateCodec(): FixedSizeCodec<
  UserHolderStateArgs,
  UserHolderState
> {
  return combineCodec(getUserHolderStateEncoder(), getUserHolderStateDecoder());
}

export function decodeUserHolderState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UserHolderState, TAddress>;
export function decodeUserHolderState<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UserHolderState, TAddress>;
export function decodeUserHolderState<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<UserHolderState, TAddress>
  | MaybeAccount<UserHolderState, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserHolderStateDecoder(),
  );
}

export async function fetchUserHolderState<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UserHolderState, TAddress>> {
  const maybeAccount = await fetchMaybeUserHolderState(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserHolderState<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UserHolderState, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserHolderState(maybeAccount);
}

export async function fetchAllUserHolderState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UserHolderState>[]> {
  const maybeAccounts = await fetchAllMaybeUserHolderState(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserHolderState(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UserHolderState>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeUserHolderState(maybeAccount),
  );
}

export function getUserHolderStateSize(): number {
  return 168;
}
