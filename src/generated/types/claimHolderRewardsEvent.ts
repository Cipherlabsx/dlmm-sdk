/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Emitted when user claims holder rewards based on staked CIPHER.
 *
 * **When Emitted:**
 * - User calls `claim_holder_rewards()`
 * - Only claimable if user has synced their stake at least once
 *
 * **Calculation:**
 * - `current_period_claimed`: Rewards from current stake period (entry_index → current_index)
 * - `pending_claimed`: Rewards accumulated from previous stake periods
 * - `amount`: Total claimed (current_period + pending)
 *
 * **Fields:**
 * - `user`: User's public key
 * - `pool`: Pool address (if pool-specific) or default for global
 * - `amount`: Total USDC (or quote token) claimed
 * - `pending_claimed`: Portion from previous periods
 * - `current_period_claimed`: Portion from current period
 * - `timestamp`: Unix timestamp of claim
 */
export type ClaimHolderRewardsEvent = {
  user: Address;
  pool: Address;
  amount: bigint;
  pendingClaimed: bigint;
  currentPeriodClaimed: bigint;
  timestamp: bigint;
};

export type ClaimHolderRewardsEventArgs = {
  user: Address;
  pool: Address;
  amount: number | bigint;
  pendingClaimed: number | bigint;
  currentPeriodClaimed: number | bigint;
  timestamp: number | bigint;
};

export function getClaimHolderRewardsEventEncoder(): FixedSizeEncoder<ClaimHolderRewardsEventArgs> {
  return getStructEncoder([
    ["user", getAddressEncoder()],
    ["pool", getAddressEncoder()],
    ["amount", getU128Encoder()],
    ["pendingClaimed", getU128Encoder()],
    ["currentPeriodClaimed", getU128Encoder()],
    ["timestamp", getI64Encoder()],
  ]);
}

export function getClaimHolderRewardsEventDecoder(): FixedSizeDecoder<ClaimHolderRewardsEvent> {
  return getStructDecoder([
    ["user", getAddressDecoder()],
    ["pool", getAddressDecoder()],
    ["amount", getU128Decoder()],
    ["pendingClaimed", getU128Decoder()],
    ["currentPeriodClaimed", getU128Decoder()],
    ["timestamp", getI64Decoder()],
  ]);
}

export function getClaimHolderRewardsEventCodec(): FixedSizeCodec<
  ClaimHolderRewardsEventArgs,
  ClaimHolderRewardsEvent
> {
  return combineCodec(
    getClaimHolderRewardsEventEncoder(),
    getClaimHolderRewardsEventDecoder(),
  );
}
